# Python 3.12 recommended.
# Install the CUDA-enabled PyTorch build separately because the exact wheel
# depends on the installed NVIDIA driver/CUDA target.
transformers==4.57.6
accelerate>=1.10,<2
safetensors>=0.5
huggingface_hub>=0.34
sentence-transformers>=5.0,<6
faiss-cpu>=1.9
numpy>=1.26,<3
scipy>=1.11
rank-bm25>=0.2
