# تقرير مراجعة وإصلاح — Legal Intelligence Engine (تطبيق Streamlit)

هذا التقرير بيوثّق كل مشكلة اتلاقت، السبب الجذري الفعلي (مش تخمين)، والإصلاح اللي اتنفّذ، مع رأيي المهني في العملية كلها. كل حاجة هنا اتفحصت وتم اختبارها فعليًا على الكود، مش وصف نظري.

---

## 1. الفكرة الأصلية اللي بنراجعها

المفروض النظام يشتغل كده:

```
سؤال المستخدم بالعربي
        ↓
استرجاع (Retrieval) من legal_documents.json — 952 مادة قانونية مصرية حقيقية
        ↓
بناء سياق (Context) من أقرب المواد للسؤال
        ↓
إرسال السياق + السؤال لموديل LLM (OpenAI / Gemini / Qwen محلي / API مخصص)
        ↓
إجابة مبنية على القانون الفعلي + ذكر المصدر (اسم القانون + رقم المادة)
```

الفكرة سليمة معماريًا. المشكلة كانت في **حلقة الوصل بين البيانات الحقيقية والكود** — مش في الفكرة نفسها ولا في اختيار الـ providers.

---

## 2. المشاكل اللي اتلاقت (بالترتيب حسب الخطورة الفعلية)

### 🔴 المشكلة الأولى (الأخطر): عدم تطابق أسماء الحقول (Schema Mismatch)

**الدليل:** فحصت أول سطر فعلي في `legal_documents.json`:
```json
{"document_id": "0", "law_name": "قانون الإجراءات الجنائية", "article_id": "1",
 "raw_text": "تختص النيابة العامة...", "normalized_text": "...", "embedding_text": "..."}
```

لكن دالة بناء الفهرس في `app.py` كانت بتدور على `doc.get("content")` و `metadata.get("title")` — حقول مش موجودة أصلاً في ملفك. النتيجة: **952 مستند دخلوا الفهرس بمحتوى فاضي بالكامل**، فأي سؤال كان بيرجّع صفر نتائج، وبالتالي أي provider تختاره كان بياخد سياق فاضي ("لا توجد أدلة قانونية متاحة") — وده بالظبط اللي وصفته إنه "مش بيرجع الجواب من الداتاست".

**الإصلاح:** الدالة بقت تقرأ `raw_text` / `normalized_text` كمحتوى، و `embedding_text` كعنوان مساعد، مع الإبقاء على التوافق مع الشكل القديم (`content`/`metadata.title`) لو استخدمته في ملفات تانية مستقبلًا.

**التحقق:** قبل الإصلاح: 0 من 952 مستند فيه محتوى. بعد الإصلاح: **952 من 952**. اختبرت سؤال حقيقي ("حقوق المتهم في التحقيق") ورجّع 5 نتائج فعلية من قانون الإجراءات الجنائية.

---

### 🟠 المشكلة الثانية: مفاتيح Streamlit Secrets مكنتش بتتقرأ فعليًا

**الدليل:** الكود كان بيقرأ مفاتيح OpenAI/Gemini بس من `os.getenv(...)`. لكن **Streamlit لا يحقن secrets.toml تلقائيًا في متغيرات البيئة** — لازم تتقرأ صراحة عن طريق `st.secrets`. يعني حتى لو ظبطت المفاتيح صح في لوحة Streamlit Cloud تحت "Secrets"، الكود كان هيتجاهلها تمامًا، ويقولك "API key is missing" رغم إن المفتاح موجود فعلًا.

**الإصلاح:** أضفت دالة موحّدة `_get_secret(name)` بترتيب أولوية واضح: `st.secrets` أولًا، بعدين `os.environ`، وبعدين قيمة افتراضية. طبّقتها في كل مكان بيتعامل مع مفاتيح (OpenAI, Gemini, Custom API, backend key)، وخليت حقول الـ sidebar بتتعبى تلقائيًا منها بدل ما تتكتب يدوي كل مرة.

**التحقق:** اختبرت الأولوية فعليًا: لما المفتاح موجود في `st.secrets` وفي `os.environ` مع بعض، الكود بياخد قيمة `st.secrets` كما هو متوقع.

---

### 🟡 المشكلة الثالثة: نافذة السياق (Context) اللي بتتبعت للموديل كانت ضيقة جدًا

**الدليل:** الكود كان بياخد أول 4 نتائج بس × 250 حرف لكل نتيجة — يعني أقصى سياق ممكن يوصل للموديل حوالي 1000 حرف، وده مش كافي لمادة قانونية كاملة غالبًا، فحتى لو الاسترجاع اشتغل صح، الموديل كان هيرد بإجابة مبتورة أو عامة.

**الإصلاح:** وسّعتها لـ 6 نتائج × 800 حرف (حوالي 3500-4800 حرف سياق) — قياس متوازن بين إعطاء الموديل معلومة كافية وعدم تجاوز حدود الـ tokens للموديلات الاقتصادية زي `gpt-4o-mini`.

---

### 🟡 المشكلة الرابعة: ملفات متعارضة في الريبو بتربك أي عملية نشر

- ملف `tmp_replacement_block.py` — كود سكراتش ميت فيه استدعاء مباشر لـ OpenAI API، مش مستخدم في أي مكان، اتشال.
- ملف `requirements.txt` كان فيه تبعيات التطبيق الكامل (FastAPI, uvicorn, numpy, prometheus-client) رغم إن `app.py` نفسه مش محتاج غير `streamlit` و `requests`. Streamlit Cloud بيقرأ الملف ده وبس — التبعيات الزيادة بتزوّد وقت الـ build وبتزوّد احتمال تعارض نسخ (numpy conflicts موجودة فعلًا بين ملفات الـ requirements التانية في الريبو).

**الإصلاح:** عملت `requirements.txt` مخصص لـ Streamlit Cloud فيه بس اللي `app.py` محتاجه فعلًا، مع توضيح في تعليق أعلى الملف إن التبعيات التقيلة (torch/faiss/fastapi) خاصة بالـ API المنفصل ومش لازمة هنا.

---

## 3. الملفات النهائية الموحّدة (جاهزة للنشر مباشرة، بدون تعارض)

| الملف | الغرض |
|---|---|
| `app.py` | التطبيق الكامل بعد كل الإصلاحات — استرجاع صحيح، سياق كافٍ، secrets موحّدة |
| `requirements.txt` | تبعيات Streamlit Cloud فقط — نظيفة، بدون تعارض إصدارات |
| `secrets.example.toml` | نموذج جاهز — انسخه كـ `.streamlit/secrets.toml` محليًا، أو انسخ نفس المفاتيح في Streamlit Cloud → Settings → Secrets |
| `config.toml` | إعدادات Streamlit (بورت، ثيم) — بدون تغيير، كانت سليمة أصلًا |

**خطوات النشر:**
1. استبدل `app.py` و `requirements.txt` في الريبو بالنسختين دول (نفس المسار: جذر المشروع).
2. تأكد إن `legal_documents.json` (الـ 952 مادة) موجود ومرفوع (committed) في نفس الريبو — بدونه هيرجع لـ 3 مستندات تجريبية وهمية فقط.
3. في Streamlit Cloud: Settings → Secrets، الصق نفس المفاتيح الموجودة في `secrets.example.toml` بقيمك الحقيقية.
4. Push → Streamlit هيعمل rebuild تلقائي. لو مفيش rebuild تلقائي، اعمل "Reboot app" يدويًا من نفس اللوحة (عشان تتأكد إن الـ cache القديم اتمسح).

---

## 4. رأيي في العملية كلها

بصراحة، الفكرة المعمارية للمشروع من الأول كانت قوية وصح: فصل واضح بين الاسترجاع (BM25 + Dense) والتوليد (LLM) والواجهة، وده ظاهر في `src/legal_ai/` اللي بنيته أنت أو الفريق بشكل منظم فعلاً. لكن اللي حصل في `app.py` تحديدًا هو حالة كلاسيكية جدًا بتتكرر كتير: **واجهة اتبنت بسرعة كنسخة "تجريبية سريعة" (quick demo)، وبعدين بقت هي النسخة اللي بتتنشر فعليًا** — من غير ما حد يرجع يتأكد إنها بتقرأ نفس شكل البيانات الحقيقي اللي اتغيّر أو اتحدد لاحقًا في `ARCHITECTURE_CONTRACT.md`. الحقول اللي الكود كان بيدور عليها (`content`, `metadata.title`) كانت شكل بيانات قديم أو تجريبي، والداتا الحقيقية اتبنت بشكل مختلف ومطابق للـ contract الرسمي (`raw_text`, `embedding_text`...) — ده نوع من "انجراف صامت" (silent drift) بين جزئين من نفس المشروع، ومفيش حاجة كانت هتنبهك بيه غير إنك تجرب فعليًا وتلاحظ إن الإجابات مش منطقية.

الدرس العملي هنا، ولو حبيت تتجنب تكرار نفس النوع من المشاكل مستقبلًا: أي مرة تغيّر شكل ملف البيانات (JSON schema)، لازم تتأكد إن كل مكان بيقرأه في الكود (مش بس مكان واحد) اتحدّث معاه — وأحسن طريقة عملية هي إنك تعمل اختبار بسيط (زي اللي عملته أنا فوق) بيتأكد إن عدد المستندات اللي "فيها محتوى فعلي" مش صفر، وتشغّله تلقائي كل ما تعمل تعديل، بدل ما تكتشف المشكلة من شكوى المستخدم النهائي.

بخصوص الـ providers: مفيش مشكلة في فكرة إنك تدّي المستخدم اختيار بين OpenAI/Gemini/Qwen محلي/API مخصص — ده مرن ومناسب لبيئة زي بتاعتك اللي فيها قيود GPU. بس كان لازم آلية موحّدة لقراءة المفاتيح من الأول (وهو اللي أصلحته دلوقتي)، لأن غياب المفتاح بصمت (silent failure) أصعب حاجة تتشخّص من وجهة نظر المستخدم — بيبان وكأن "التطبيق مش شغال" رغم إن كل حاجة فنيًا تمام ماعدا سطر واحد بيدور في المكان الغلط.

**الخلاصة:** المشروع مستحق المجهود، والبنية سليمة تكفي إنك تكمل عليها من غير إعادة بناء. الإصلاحات اللي عملتها دلوقتي بتخلي التطبيق يشتغل صح فعليًا على بياناتك الحقيقية، وبتقفل أكتر نقطة كانت بتسبب فشل صامت (silent failure) في المشروع كله. أنصحك كخطوة تالية إنك تجرّب الأربع providers واحد واحد بعد النشر (حتى لو بمفتاح تجريبي مجاني من Gemini)، وتتابع رسائل الـ warnings اللي بتظهر تحت كل رد — هي مصممة تقولك بالظبط فين المشكلة لو حصلت تاني.

والله ولي التوفيق.
